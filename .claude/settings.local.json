{
  "permissions": {
    "allow": [
      "Bash(python3 -m venv .venv)",
      "Bash(.venv/bin/pip install:*)",
      "Bash(.venv/bin/pytest -v)",
      "Bash(.venv/bin/python -m pytest -v)",
      "Bash(.venv/bin/python -m pytest tests/test_coordinator.py::test_explicit_abort_releases_locks -vv)",
      "Bash(.venv/bin/python -c \"import coordinator.main, shard.main, common.locks, common.wal, common.hashing, common.config; print\\('all modules import cleanly'\\)\")",
      "Bash(WAL_DIR=/tmp .venv/bin/python -c \"import coordinator.main, shard.main, common.locks, common.wal, common.hashing, common.config; print\\('all modules import cleanly'\\)\")",
      "Bash(.venv/bin/python -c \"import rich; print\\(rich.__version__\\)\")",
      "Bash(.venv/bin/python -c \"from rich.live import Live; from rich.layout import Layout; from rich.panel import Panel; from rich.table import Table; print\\('rich imports OK'\\)\")",
      "Bash(.venv/bin/python -m pytest)",
      "Bash(WAL_DIR=/tmp .venv/bin/python -c \"import dashboard, coordinator.main, shard.main, common.locks, common.wal, common.hashing, common.config; print\\('all modules import'\\)\")"
    ]
  }
}
